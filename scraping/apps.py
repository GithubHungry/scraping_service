from django.apps import AppConfig


class ScrapingConfig(AppConfig):
    name = 'scraping'
    verbose_name = 'Job search application'  # for changing grout name of app scraping -> then change __init__.py
